//import react since you are testing snapshots
import React from 'react';
// Import the component to take a snapshot of.
import Home from './Home';;
//import the default from the react-test-renderer that renders component to take a snapshot of.
import renderer from 'react-test-renderer';
//import axios for doing asynchronous code.
import axios from 'axios';



//define a test that tests a promise
test('Test the promise and check if the data is recieved', () => {
    
})